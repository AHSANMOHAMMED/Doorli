import { useState } from 'react';
import { ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { useRouter } from 'expo-router';
import { apiClient } from '../../lib/axios';
import { GlassButton } from '../../components/GlassButton';
import { DoorliColors } from '../../constants/colors';
export default function MobilityScreen() { const router = useRouter(); const [from, setFrom] = useState('Colombo'); const [to, setTo] = useState('Galle'); const [message, setMessage] = useState(''); async function plan() { try { const r = await apiClient.get(`/transit/journey-plan?from=${from}&to=${to}`); setMessage(r.data.data?.message ?? `${r.data.data?.totalDurationMins ?? 0} minutes · LKR ${r.data.data?.totalFare ?? 0}`); } catch (e) { setMessage(e instanceof Error ? e.message : 'Planner unavailable.'); } } return <ScrollView style={styles.page} contentContainerStyle={styles.content}><Text style={styles.eyebrow}>DAILY MOVEMENT</Text><Text style={styles.title}>Mobility hub</Text><View style={styles.panel}><Text style={styles.heading}>Plan a journey</Text><TextInput value={from} onChangeText={setFrom} style={styles.input} placeholder="From" placeholderTextColor="#6b86a6" /><TextInput value={to} onChangeText={setTo} style={styles.input} placeholder="To" placeholderTextColor="#6b86a6" /><GlassButton title="Plan journey" onPress={plan} /><GlassButton title="Request a ride" variant="secondary" onPress={() => router.push('/(customer)/ride-booking')} /><GlassButton title="Bus tickets" variant="secondary" onPress={() => router.push('/(customer)/bus-ticket' as never)} />{message ? <Text style={styles.message}>{message}</Text> : null}</View></ScrollView>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: DoorliColors.deep }, content: { padding: 20, paddingTop: 55, gap: 16 }, eyebrow: { color: DoorliColors.mint, fontSize: 11, letterSpacing: 2 }, title: { color: '#fff', fontSize: 32, fontWeight: '800' }, panel: { backgroundColor: 'rgba(255,255,255,.08)', borderRadius: 24, padding: 20, gap: 12 }, heading: { color: '#fff', fontSize: 20, fontWeight: '700' }, input: { backgroundColor: 'rgba(0,0,0,.2)', color: '#fff', borderRadius: 14, padding: 15, fontSize: 16 }, message: { color: DoorliColors.mint, fontSize: 13 } });
