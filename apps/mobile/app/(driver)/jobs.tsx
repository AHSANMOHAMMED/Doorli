import { View, Text } from 'react-native';

export default function DriverJobs() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Driver jobs — Week 11–12</Text>
    </View>
  );
}
