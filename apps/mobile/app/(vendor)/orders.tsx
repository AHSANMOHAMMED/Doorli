import { View, Text } from 'react-native';

export default function VendorOrders() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Vendor orders — Week 5–6</Text>
    </View>
  );
}
