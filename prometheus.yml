global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    environment: 'production'
    app: 'doorli'

rule_files:
  - 'alerts.yml'

scrape_configs:
  - job_name: 'api-gateway'
    static_configs:
       - targets: ['api:3001']
    metrics_path: /metrics
